configs:
  cm:
    extension.config: |
      extensions:
        - name: otel-extension
          backend:
            services:
              - url: http://argocd-extension-backend-api.glueops-core.svc.cluster.local:8000
