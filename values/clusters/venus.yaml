configs:
  cm:
    extension.config: |
      extensions:
        - name: otel-extension
          backend:
            services:
              # Backend is deployed from GlueOps/argo-cd-extention-backend
              # (service argocd-extension-backend-api, namespace glueops-core),
              # which implements /api/links. This repo no longer ships a backend.
              - url: http://argocd-extension-backend-api.glueops-core.svc.cluster.local:8000
